services:
  - type: web
    name: pacman-dqn
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --chdir backend app:app --timeout 120 --workers 1
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.0
